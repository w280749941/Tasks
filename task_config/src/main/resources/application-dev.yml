spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/
          username:
          password:
          basedir: G:\Projects\GitConfigBase\
  rabbitmq:
    host: 192.168.1.233
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
management:
  endpoints:
    web:
      exposure:
        include: "*"